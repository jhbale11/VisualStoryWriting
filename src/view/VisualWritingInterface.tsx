// Placeholder component. The original VisualWritingInterface was archived under legacy/ and is not part of the translation workflow.
const VisualWritingInterface = () => (
  <div style={{ padding: 16 }}>
    Visual writing interface is disabled in this build.
  </div>
);

export default VisualWritingInterface;

